# Server lives outside the public plugin repo — kept local only
worker/

# Cloudflare Wrangler local state — contains your account id + email, never commit
.wrangler/
worker/.wrangler/

# Private internal notes — deployment/account details, keep local
inst.md

# Local secrets / auth (the castle login token lives in ~/.castle, not here — but just in case)
.castle/
.dev.vars
*.env
.env
.env.*

# Dependencies / build
node_modules/
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
